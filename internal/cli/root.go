// Package cli wires cobra commands and flags to the worktree domain.
package cli

// Execute is the CLI entry point. It will be implemented in Phase 3.
// For now it is a stub so that cmd/wt/main.go compiles during Phase 1.
func Execute(version string) error {
	_ = version
	return nil
}
